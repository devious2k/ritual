{
  "buildCommand": "cd ../.. && pnpm install --filter @lodgekey/web... --no-frozen-lockfile && pnpm --filter @lodgekey/web build",
  "installCommand": "echo skip",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
